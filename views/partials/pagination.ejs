<div class="mt-6 flex justify-center space-x-2">
  <% for (let i = 1; i <= pages; i++) { %>
    <a href="/?page=<%= i %>&q=<%= keyword %>"
      class="px-3 py-1 rounded border <%= i === page ? 'bg-blue-500 text-white' : 'bg-white text-gray-700' %>">
      <%= i %>
    </a>
  <% } %>
</div>
